name: CI/CD Pipeline

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

env:
  NODE_VERSION: '20.x'
  PROJECT_DIR: better-tomorrow-school

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install root dependencies
        working-directory: ${{ env.PROJECT_DIR }}
        run: npm ci

      - name: Install server dependencies
        working-directory: ${{ env.PROJECT_DIR }}/server
        run: npm ci

      - name: Install client dependencies
        working-directory: ${{ env.PROJECT_DIR }}/client
        run: npm ci

      - name: Generate Prisma Client
        working-directory: ${{ env.PROJECT_DIR }}/server
        run: npx prisma generate

      - name: Run server lint
        working-directory: ${{ env.PROJECT_DIR }}/server
        run: npm run lint

      - name: Run client lint
        working-directory: ${{ env.PROJECT_DIR }}/client
        run: npm run lint

      - name: Type check server
        working-directory: ${{ env.PROJECT_DIR }}/server
        run: npx tsc --noEmit

      - name: Type check client
        working-directory: ${{ env.PROJECT_DIR }}/client
        run: npx tsc --noEmit

  build:
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        working-directory: ${{ env.PROJECT_DIR }}
        run: |
          npm ci
          cd server && npm ci
          cd ../client && npm ci

      - name: Generate Prisma Client
        working-directory: ${{ env.PROJECT_DIR }}/server
        run: npx prisma generate

      - name: Build server
        working-directory: ${{ env.PROJECT_DIR }}/server
        run: npm run build

      - name: Build client
        working-directory: ${{ env.PROJECT_DIR }}/client
        run: npm run build

      - name: Upload server build artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-dist
          path: ${{ env.PROJECT_DIR }}/server/dist/
          retention-days: 1

      - name: Upload client build artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-dist
          path: ${{ env.PROJECT_DIR }}/client/dist/
          retention-days: 1

  deploy-api:
    needs: build
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download server build
        uses: actions/download-artifact@v4
        with:
          name: server-dist
          path: ${{ env.PROJECT_DIR }}/server/dist/

      - name: Deploy to Render
        uses: render-actions/deploy@v1
        with:
          service-id: ${{ secrets.RENDER_API_SERVICE_ID }}
          api-key: ${{ secrets.RENDER_API_KEY }}

  deploy-web:
    needs: build
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download client build
        uses: actions/download-artifact@v4
        with:
          name: client-dist
          path: ${{ env.PROJECT_DIR }}/client/dist/

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ${{ env.PROJECT_DIR }}/client
          vercel-args: '--prod'